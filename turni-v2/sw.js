self.addEventListener('push',event=>{let d={};try{d=event.data?.json()||{}}catch{d={message:event.data?.text()||''}};event.waitUntil(self.registration.showNotification(d.title||'Eurospin Torre Maura',{body:d.message||'',icon:'',badge:'',data:{url:d.url||'/Portafoglio/turni-v2/#announcements'},tag:'eurospin-turni-'+Date.now(),renotify:true}))});
self.addEventListener('notificationclick',event=>{event.notification.close();const url=event.notification.data?.url||'/Portafoglio/turni-v2/#announcements';event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const c of list){if('focus'in c){c.navigate(url);return c.focus()}}return clients.openWindow(url)}))});
