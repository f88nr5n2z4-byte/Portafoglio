(function(){
 const H=()=>({apikey:CLOUD.key,Authorization:'Bearer '+CLOUD.key,'Content-Type':'application/json'}),B=()=>CLOUD.url+'/rest/v1/'+encodeURIComponent(CLOUD.table);
 const label={classificare:'Non deciso',mia:'Personale',dividere:'Da dividere'};
 function isExpense(m){return m.type==='expense'}
 function sharedOpen(){return state.movements.filter(m=>isExpense(m)&&m.shareType==='dividere'&&!m.settled)}
 function ensureUI(){const h=$('home');if(!h)return;if(!$('sharePanel')){const p=document.createElement('div');p.id='sharePanel';p.className='card';p.style.marginBottom='14px';p.innerHTML='<div class="row"><div><div class="label">DA RIMBORSARE</div><div class="big" id="shareDue" style="font-size:32px;margin:5px 0">€0,00</div><small class="label" id="shareInfo"></small></div><button class="primary" id="shareSettle" onclick="settleShared()">✓ Registra rimborso</button></div>';h.insertBefore(p,h.querySelector('.grid'))}if(!$('homeLedger')){const r=document.createElement('div');r.id='homeLedger';r.className='card';r.style.marginBottom='14px';r.innerHTML='<div><h3 style="margin:0">Registro spese</h3><div class="label" style="margin-top:4px">Tocca una destinazione per spostare il movimento</div></div><div class="ledgerGrid"><section><div class="ledgerHead todo">⚠️ Non deciso <b id="cntTodo">0</b></div><div id="ledgerTodo"></div></section><section><div class="ledgerHead share">🤝 Da dividere <b id="cntShare">0</b></div><div id="ledgerShare"></div></section><section><div class="ledgerHead mine">👤 Personali <b id="cntMine">0</b></div><div id="ledgerMine"></div></section></div><details class="settledDetails"><summary>🔒 Già saldati <span id="cntSettled">0</span></summary><div id="ledgerSettled"></div></details>';h.insertBefore(r,h.querySelector('.grid'))}if(!$('ledgerStyle')){const s=document.createElement('style');s.id='ledgerStyle';s.textContent='.ledgerGrid{display:grid!important;grid-template-columns:repeat(3,minmax(0,1fr))!important;gap:7px;margin-top:14px;width:100%}.ledgerGrid section{min-width:0;border:1px solid #262e3a;border-radius:14px;padding:7px;background:#0e1219;overflow:hidden}.ledgerHead{display:flex;flex-direction:column;gap:2px;min-height:34px;font-size:10px;font-weight:900;margin-bottom:7px}.ledgerHead b{font-size:10px}.ledgerHead.todo{color:#ffd76b}.ledgerHead.share{color:var(--green)}.ledgerHead.mine{color:#93a0b4}.ledgerItem{border:1px solid #29313f;border-radius:10px;padding:7px;margin-bottom:7px;background:#151a23;min-width:0}.ledgerTop{display:block}.ledgerName{font-weight:850;font-size:11px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.ledgerAmount{font-weight:900;font-size:12px;margin-top:4px}.ledgerMeta{font-size:9px;color:#93a0b4;margin-top:3px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.ledgerActions{display:flex;flex-direction:column;gap:4px;margin-top:7px}.ledgerActions button{width:100%;border:1px solid #303847;background:#11161f;color:#cfd6e1;border-radius:8px;padding:6px 1px;font-size:9px;font-weight:850;line-height:1.1}.ledgerActions button.on{border-color:var(--green);color:var(--green);background:rgba(100,230,149,.09)}.ledgerEmpty{color:#697586;font-size:9px;padding:8px 1px}.settledDetails{margin-top:12px;border-top:1px solid #252c38;padding-top:10px}.settledDetails summary{cursor:pointer;color:#6ea8ff;font-weight:850;font-size:12px}.settledLocked{opacity:.7}@media(min-width:761px){.ledgerGrid{gap:12px}.ledgerGrid section{padding:11px}.ledgerHead{flex-direction:row;justify-content:space-between;font-size:12px}.ledgerName{font-size:13px}.ledgerAmount{font-size:13px}.ledgerMeta{font-size:10px}.ledgerActions{display:grid;grid-template-columns:repeat(3,1fr)}.ledgerActions button{font-size:10px}}@media(max-width:380px){.ledgerGrid{gap:4px}.ledgerGrid section{padding:5px}.ledgerActions button{font-size:8px}.ledgerName{font-size:10px}}';document.head.appendChild(s)}}
 function itemHTML(m,locked=false){let sh=m.shareType||'classificare';return `<div class="ledgerItem ${locked?'settledLocked':''}"><div class="ledgerTop"><div class="ledgerName">${esc(m.description||m.category)}</div><div class="ledgerMeta">${new Date(m.date+'T12:00').toLocaleDateString('it-IT')}</div><div class="ledgerAmount">-${money(m.amount)}</div></div>${locked?'<div class="ledgerMeta" style="color:#6ea8ff;margin-top:7px">🔒 Chiuso</div>':`<div class="ledgerActions"><button class="${sh==='classificare'?'on':''}" onclick="quickClassify(${m.id},'classificare',event)">Non deciso</button><button class="${sh==='dividere'?'on':''}" onclick="quickClassify(${m.id},'dividere',event)">Dividere</button><button class="${sh==='mia'?'on':''}" onclick="quickClassify(${m.id},'mia',event)">Personale</button></div>`}</div>`}
 function renderLedger(){ensureUI();let e=state.movements.filter(isExpense).sort((a,b)=>b.date.localeCompare(a.date)||b.id-a.id),todo=e.filter(m=>(m.shareType||'classificare')==='classificare'&&!m.settled),share=e.filter(m=>m.shareType==='dividere'&&!m.settled),mine=e.filter(m=>m.shareType==='mia'),done=e.filter(m=>m.shareType==='dividere'&&m.settled),put=(id,a,l=false)=>$(id).innerHTML=a.length?a.map(m=>itemHTML(m,l)).join(''):'<div class="ledgerEmpty">Vuoto</div>';put('ledgerTodo',todo);put('ledgerShare',share);put('ledgerMine',mine);put('ledgerSettled',done,true);$('cntTodo').textContent=todo.length;$('cntShare').textContent=share.length;$('cntMine').textContent=mine.length;$('cntSettled').textContent=done.length;let gross=share.reduce((a,m)=>a+Number(m.amount||0),0);$('shareDue').textContent=money(gross/2);$('shareInfo').textContent=share.length?`${money(gross)} di spese aperte · metà ${money(gross/2)}`:'Nessuna spesa da dividere';$('shareSettle').disabled=!share.length}
 const baseRender=render;render=function(){baseRender();renderLedger()};
 async function patch(m){if(!m.cloudId)return;let r=await fetch(B()+'?Id=eq.'+encodeURIComponent(m.cloudId),{method:'PATCH',headers:H(),body:JSON.stringify({Condivisione:m.shareType||'classificare',Saldato:!!m.settled})});if(!r.ok)throw 0}
 window.quickClassify=async function(id,v,ev){if(ev){ev.preventDefault();ev.stopPropagation()}let m=state.movements.find(x=>x.id===id);if(!m||!isExpense(m))return;if(m.settled&&m.shareType==='dividere')return toast('Questa spesa è già saldata e bloccata');m.shareType=v;m.settled=false;save();renderLedger();toast('Spostata in '+label[v]);try{await patch(m)}catch{toast('Salvata sul telefono, sync non riuscita')}};
 window.settleShared=async function(){let ms=sharedOpen();if(!ms.length)return;let gross=ms.reduce((a,m)=>a+Number(m.amount||0),0),raw=prompt(`Totale spese da dividere: ${money(gross)}\nQuanto hai ricevuto?`,(gross/2).toFixed(2).replace('.',','));if(raw===null)return;let refund=parseFloat(String(raw).replace(',','.'));if(!Number.isFinite(refund)||refund<0)return toast('Importo non valido');if(!confirm(`Ricevuti ${money(refund)}. Chiudere definitivamente ${ms.length} ${ms.length===1?'spesa':'spese'}?`))return;let stamp=new Date().toISOString(),group='saldo-'+Date.now();ms.forEach(m=>{m.settled=true;m.settlementGroup=group;m.settledAt=stamp});state.movements.push({id:Date.now()+1,type:'income',amount:refund,category:'Altro',date:today(),description:'Rimborso spese condivise',source:'settlement',shareType:'mia'});save();let ids=ms.filter(m=>m.cloudId).map(m=>m.cloudId);try{if(ids.length){let r=await fetch(B()+'?Id=in.('+ids.map(encodeURIComponent).join(',')+')',{method:'PATCH',headers:H(),body:JSON.stringify({Saldato:true,Saldata_il:stamp,Saldo_gruppo:group})});if(!r.ok)throw 0}let r2=await fetch(B(),{method:'POST',headers:{...H(),Prefer:'return=minimal'},body:JSON.stringify({Importo:refund,Esercente:'Rimborso spese condivise',Categoria:'Altro',Tipo:'income',Condivisione:'mia',Saldato:true,Saldo_gruppo:group,Rimborso:refund})});if(!r2.ok)throw 0;toast('Rimborso registrato')}catch{toast('Chiuso sul telefono, sincronizzazione parziale')}};
 async function sync(){if(!window.CLOUD?.url)return;try{let r=await fetch(B()+'?select=Id,Created_at,Importo,Esercente,Categoria,Tipo,Condivisione,Saldato,Saldata_il,Saldo_gruppo&order=Created_at.desc',{headers:H()});if(!r.ok)throw 0;let rows=await r.json(),live=new Set(),changed=false;for(let row of rows){let cid=String(row.Id),amount=+row.Importo;live.add(cid);if(!amount||amount<=0)continue;let merchant=row.Esercente||'Pagamento Wallet',d=row.Created_at?new Date(row.Created_at):new Date(),date=Number.isNaN(d.getTime())?today():`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`,db=(row.Categoria||'').trim(),category=db&&db.toLowerCase()!=='altro'?db:guessCategory(merchant),next={type:row.Tipo||'expense',amount,category,date,description:merchant,shareType:row.Condivisione||((row.Tipo||'expense')==='expense'?'classificare':'mia'),settled:!!row.Saldato,settlementGroup:row.Saldo_gruppo||null,settledAt:row.Saldata_il||null},m=state.movements.find(x=>String(x.cloudId||'')===cid);if(!m){state.movements.push({id:Date.now()+Math.floor(Math.random()*99999),cloudId:cid,source:row.Tipo==='income'&&row.Saldo_gruppo?'settlement':'supabase',...next});changed=true}else if(Object.keys(next).some(k=>String(m[k]??'')!==String(next[k]??''))){Object.assign(m,next);changed=true}}let before=state.movements.length;state.movements=state.movements.filter(m=>m.source!=='supabase'||!m.cloudId||live.has(String(m.cloudId)));if(before!==state.movements.length)changed=true;if(changed){save(false);render();toast('Spese sincronizzate')}else renderLedger()}catch(e){console.warn('Sync cloud non riuscita')}}
 window.syncCloudMovements=sync;ensureUI();renderLedger();if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(sync,350));else setTimeout(sync,350);window.addEventListener('focus',sync);document.addEventListener('visibilitychange',()=>{if(!document.hidden)sync()});
})();