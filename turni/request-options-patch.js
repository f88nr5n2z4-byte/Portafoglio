syncPushOwner();openShiftRequest=function(dayIndex){const w=state.data.weeks[state.week],n=state.user.employee,s=w.schedule[n][dayIndex],d=w.dates[dayIndex],opts=SHIFT_OPTIONS.filter(x=>!['RIPOSO','MATERNITÀ','MALATTIA','—'].includes(x));document.body.insertAdjacentHTML('beforeend',`<div class="modal-backdrop" id="shiftModal"><div class="shift-modal"><div class="modal-grab"></div><h3>Richiesta cambio turno</h3><div class="current-shift"><small>${esc(fmtDate(d))}</small><b style="display:block;margin-top:4px">Turno attuale: ${esc(s)}</b></div><div class="field"><label>Cosa vorresti fare</label><select id="desiredShift">${opts.map(x=>`<option>${esc(x)}</option>`).join('')}</select></div><div class="request-choice"><button id="presetBtn" class="active">Scelta rapida</button><button id="customBtn">Personalizza</button></div><div class="custom-request" id="customBox"><b>Prima fascia</b><div class="time-row"><div class="field"><label>Inizio</label><input id="c1s" type="time" step="900" value="07:00"></div><div class="field"><label>Fine</label><input id="c1e" type="time" step="900" value="14:00"></div></div><b>Seconda fascia <small>(opzionale)</small></b><div class="time-row"><div class="field"><label>Inizio</label><input id="c2s" type="time" step="900"></div><div class="field"><label>Fine</label><input id="c2e" type="time" step="900"></div></div><small>Puoi inventare qualsiasi spezzato.</small></div><div class="modal-actions"><button class="modal-close" id="cancelReq">Annulla</button><button class="modal-send" id="sendReq">Invia richiesta</button></div><div id="reqResult"></div></div></div>`);let custom=false;const box=document.getElementById('customBox'),pb=document.getElementById('presetBtn'),cb=document.getElementById('customBtn');pb.onclick=()=>{custom=false;box.classList.remove('show');pb.classList.add('active');cb.classList.remove('active')};cb.onclick=()=>{custom=true;box.classList.add('show');cb.classList.add('active');pb.classList.remove('active')};document.getElementById('cancelReq').onclick=()=>document.getElementById('shiftModal').remove();document.getElementById('sendReq').onclick=async()=>{let wanted=document.getElementById('desiredShift').value;if(custom){const a=document.getElementById('c1s').value,b=document.getElementById('c1e').value,c=document.getElementById('c2s').value,z=document.getElementById('c2e').value;if(!a||!b||hm(b)<=hm(a)){alert('Controlla la prima fascia.');return}wanted=`${a}-${b}`;if(c||z){if(!c||!z||hm(z)<=hm(c)||hm(c)<hm(b)){alert('Controlla la seconda fascia.');return}wanted+=` / ${c}-${z}`}}const title=wanted==='FERIE'?'RICHIESTA FERIE':wanted==='PERMESSO'?'RICHIESTA PERMESSO':'RICHIESTA CAMBIO TURNO';const message=`${title}\nGiorno: ${fmtDate(d)}\nTurno attuale: ${s}\nTurno richiesto: ${wanted}`;const btn=document.getElementById('sendReq');btn.disabled=true;btn.textContent='Invio…';try{await api('create_request',{method:'POST',body:{message}});document.getElementById('reqResult').innerHTML='<div class="success">Richiesta inviata a Eurospin.</div>';setTimeout(()=>document.getElementById('shiftModal')?.remove(),800)}catch(e){document.getElementById('reqResult').innerHTML=`<div class="error">${esc(e.message)}</div>`;btn.disabled=false;btn.textContent='Invia richiesta'}}};