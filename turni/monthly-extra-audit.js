(()=>{
'use strict';
const ROOT=document.getElementById('app');if(!ROOT)return;
const NAMES=['Umberto','Fabio','Emanuele','Stefania B','Giada','Giuliano','Daniele','Manuel','Romina','Stefania F','Paolo','Marco'];
const RESP=['Umberto','Fabio','Emanuele'];
const OFF=new Set(['RIPOSO','FERIE','MALATTIA','MATERNITÀ','PERMESSO','—']);
const works=s=>{if(!s||OFF.has(s))return false;return String(s).split('/').some(x=>{const p=x.trim().split('-').map(v=>v.trim());return p.length===2&&Number.isFinite(hm(p[0]))&&Number.isFinite(hm(p[1]))&&hm(p[1])>hm(p[0])})};
const monday=d=>{const x=new Date(d+'T12:00:00'),k=(x.getDay()+6)%7;x.setDate(x.getDate()-k);return x.toLocaleDateString('sv-SE')};
function readDraft(){try{return JSON.parse(localStorage.getItem('tm_planner_draft')||'null')?.week||null}catch{return null}}
function addProblem(msgs,msg){if(!msgs.includes(msg))msgs.push(msg)}
async function run(){const w=readDraft();if(!w?.dates?.length||!w.schedule)return;let official={weeks:[]};try{const r=await api('get_schedule');if(r.data&&Array.isArray(r.data.weeks))official=r.data}catch{}
 const problems=[];
 // Controllo sequenza continua in entrambe le direzioni quando esistono dati pubblicati prima/dopo il mese.
 for(const n of NAMES){const map=new Map();for(const pw of (official.weeks||[]))for(let i=0;i<(pw.dates||[]).length;i++)map.set(pw.dates[i],pw.schedule?.[n]?.[i]);for(let i=0;i<w.dates.length;i++)map.set(w.dates[i],w.schedule?.[n]?.[i]);const rows=[...map].map(([d,s])=>({d,s,inDraft:w.dates.includes(d)})).sort((a,b)=>a.d.localeCompare(b.d));let streak=0,last=null,touches=false;for(const row of rows){if(last){const gap=(new Date(row.d+'T12:00:00')-new Date(last+'T12:00:00'))/86400000;if(gap!==1){streak=0;touches=false}}if(works(row.s)){streak++;touches=touches||row.inDraft;if(streak>6&&touches)addProblem(problems,`${n}: oltre 6 giorni consecutivi anche considerando il periodo confinante`)}else{streak=0;touches=false}last=row.d}}
 // Se un responsabile è a RIPOSO, gli altri due devono fare esattamente apertura e chiusura. Più persone possono riposare nello stesso giorno.
 for(let i=0;i<w.dates.length;i++){const d=new Date(w.dates[i]+'T12:00:00');if(d.getDay()===0)continue;const resting=RESP.filter(n=>w.schedule?.[n]?.[i]==='RIPOSO');if(resting.length===1){const other=RESP.filter(n=>n!==resting[0]),shifts=other.map(n=>w.schedule?.[n]?.[i]);if(!(shifts.includes('06:30-13:30')&&shifts.includes('13:30-20:30')))addProblem(problems,`${w.dates[i]}: con ${resting[0]} a RIPOSO, gli altri due responsabili devono fare 06:30-13:30 e 13:30-20:30`)}}
 // Chi lavora domenica deve avere il RIPOSO compensativo/infrasettimanale nella stessa settimana. Non esiste alcun limite al numero di persone che possono riposare nello stesso giorno.
 for(let i=0;i<w.dates.length;i++){const d=new Date(w.dates[i]+'T12:00:00');if(d.getDay()!==0)continue;const key=monday(w.dates[i]);for(const n of NAMES){if(!works(w.schedule?.[n]?.[i]))continue;let rests=0;for(let k=0;k<w.dates.length;k++)if(monday(w.dates[k])===key&&w.schedule?.[n]?.[k]==='RIPOSO')rests++;for(const pw of (official.weeks||[]))for(let k=0;k<(pw.dates||[]).length;k++)if(!w.dates.includes(pw.dates[k])&&monday(pw.dates[k])===key&&pw.schedule?.[n]?.[k]==='RIPOSO')rests++;if(rests===0)addProblem(problems,`${n}: lavora domenica senza RIPOSO compensativo`);if(rests>1)addProblem(problems,`${n}: più di un RIPOSO personale nella settimana della domenica lavorata`)}}
 // Nessuna settimana pubblicata dovrebbe contenere date duplicate dopo la fusione mensile.
 for(const pw of (official.weeks||[])){const a=pw.dates||[];if(new Set(a).size!==a.length)addProblem(problems,`Archivio: date duplicate nella settimana ${pw.label||a[0]||''}`)}
 const card=[...ROOT.querySelectorAll('.card')].find(c=>c.querySelector('h3')?.textContent.includes('Controllo finale'));if(!card)return;card.querySelector('.extra-audit')?.remove();const box=document.createElement('div');box.className=problems.length?'statusbad extra-audit':'statusok extra-audit';box.style.marginTop='8px';box.innerHTML=problems.length?`Controllo aggiuntivo: ${problems.length} problema/i.<br>${problems.slice(0,5).map(x=>'• '+esc(x)).join('<br>')}`:'✓ Controllo aggiuntivo a cavallo mese e responsabili superato.';card.appendChild(box);const pub=document.getElementById('publish');if(pub&&problems.length){pub.disabled=true;pub.textContent='Correggere anche il controllo aggiuntivo'}
 }
 let timer=null;const schedule=()=>{clearTimeout(timer);timer=setTimeout(run,120)};new MutationObserver(schedule).observe(ROOT,{childList:true,subtree:true});window.addEventListener('load',schedule);schedule();
})();