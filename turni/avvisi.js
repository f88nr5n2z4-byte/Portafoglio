const u=requireUser(false),app=document.getElementById('app');if(u){app.innerHTML=`<div class="app-shell"><div class="brandbar"></div><header class="topbar"><div class="toprow"><a class="back" href="index.html">‹ Turni</a><div class="brand-mini"><div class="mini-mark">E</div><div><strong>Avvisi</strong><span>Eurospin Torre Maura</span></div></div><button class="logout" onclick="logout()">Esci</button></div></header><main class="main"><section class="hero"><div class="eyebrow">Comunicazioni negozio</div><h2>Avvisi</h2><p>Messaggi condivisi con tutti i dipendenti.</p></section>${u.admin?`<section class="request-card"><div class="field"><label>Nuovo avviso</label><textarea id="msg" class="bigtextarea" maxlength="2000" placeholder="Es. Mercoledì inventario alle 6:00"></textarea></div><button class="primary" id="send">Pubblica avviso</button><div id="res"></div></section>`:''}<div class="section-title"><h3>Avvisi pubblicati</h3><span>Più recenti prima</span></div><div id="list" class="request-list"><div class="empty">Caricamento…</div></div></main></div>`;async function draw(){try{const r=await api('list_announcements'),list=r.announcements||[];document.getElementById('list').innerHTML=list.length?list.map(x=>`<article class="request-item"><div class="request-meta">${esc(formatDateTime(x.created_at))}</div><p>${esc(x.message)}</p></article>`).join(''):'<div class="empty">Nessun avviso pubblicato.</div>'}catch(e){document.getElementById('list').innerHTML=`<div class="error">${esc(e.message)}</div>`}}draw();if(u.admin)document.getElementById('send').onclick=async()=>{const message=document.getElementById('msg').value.trim();if(!message)return;const b=document.getElementById('send');b.disabled=true;b.textContent='Pubblico…';try{await api('create_announcement',{method:'POST',body:{message}});document.getElementById('msg').value='';document.getElementById('res').innerHTML='<div class="success">Avviso pubblicato a tutti.</div>';await draw()}catch(e){document.getElementById('res').innerHTML=`<div class="error">${esc(e.message)}</div>`}finally{b.disabled=false;b.textContent='Pubblica avviso'}}}