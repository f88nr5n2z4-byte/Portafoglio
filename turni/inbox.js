const u=requireUser(true),app=document.getElementById('app');if(u){const list=requestStore().sort((a,b)=>b.iso.localeCompare(a.iso));app.innerHTML=`<div class="app-shell"><div class="brandbar"></div><header class="topbar"><div class="toprow"><a class="back" href="index.html">‹ Turni</a><div class="brand-mini"><div class="mini-mark">E</div><div><strong>Richieste ricevute</strong><span>Solo account Eurospin</span></div></div><button class="logout" onclick="logout()">Esci</button></div></header><main class="main"><section class="hero inboxhero"><div class="eyebrow">Messaggi dipendenti</div><h2>Tutte le richieste</h2><p>Ordinate dalla più recente.</p></section><div class="notice">In questa versione le richieste visualizzate sono quelle salvate sul dispositivo corrente. La sincronizzazione tra telefoni richiede il backend condiviso.</div><div class="request-list">${list.length?list.map(r=>`<article class="request-item"><div class="request-meta">${esc(r.displayDate)} · <b>${esc(r.name)}</b></div><p>${esc(r.message)}</p></article>`).join(''):'<div class="empty">Nessuna richiesta presente su questo dispositivo.</div>'}</div></main></div>`}