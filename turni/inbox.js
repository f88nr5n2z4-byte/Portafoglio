const u=requireUser(true),app=document.getElementById('app');if(u){app.innerHTML=`<div class="app-shell"><div class="brandbar"></div><header class="topbar"><div class="toprow"><a class="back" href="index.html">‹ Turni</a><div class="brand-mini"><div class="mini-mark">E</div><div><strong>Richieste ricevute</strong><span>Solo account Eurospin</span></div></div><button class="logout" onclick="logout()">Esci</button></div></header><main class="main"><section class="hero inboxhero"><div class="eyebrow">Messaggi dipendenti</div><h2>Tutte le richieste</h2><p>Accetta, rifiuta o rispondi.</p></section><div class="section-title"><h3>Richieste</h3><span id="count">Caricamento…</span></div><div id="inbox" class="request-list"><div class="empty">Caricamento…</div></div></main></div>`;async function load(){try{const r=await api('list_requests'),list=r.requests||[];document.getElementById('count').textContent=`${list.length} totali`;document.getElementById('inbox').innerHTML=list.length?list.map(x=>`<article class="request-item"><div class="request-meta">${esc(formatDateTime(x.created_at))} · <b>${esc(x.employee)}</b></div><div class="request-top">${statusBadge(x.status)}</div><p>${esc(x.message)}</p><div class="request-admin"><select data-status="${x.id}"><option ${x.status==='DA VALUTARE'?'selected':''}>DA VALUTARE</option><option ${x.status==='ACCETTATA'?'selected':''}>ACCETTATA</option><option ${x.status==='RIFIUTATA'?'selected':''}>RIFIUTATA</option></select><textarea data-reply="${x.id}" placeholder="Risposta al dipendente">${esc(x.reply||'')}</textarea><button class="primary smallbtn" data-save="${x.id}">Salva risposta</button></div></article>`).join(''):'<div class="empty">Nessuna richiesta ricevuta.</div>';document.querySelectorAll('[data-save]').forEach(b=>b.onclick=async()=>{const id=+b.dataset.save,status=document.querySelector(`[data-status="${id}"]`).value,reply=document.querySelector(`[data-reply="${id}"]`).value;b.disabled=true;b.textContent='Salvo…';try{await api('update_request',{method:'POST',body:{id,status,reply}});await load()}catch(e){alert(e.message)}})}catch(e){document.getElementById('inbox').innerHTML=`<div class="error">${esc(e.message)}</div>`}}load()}