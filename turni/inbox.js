const u=requireUser(true),app=document.getElementById('app');let all=[],mode='open';
const MONTHS={gen:0,feb:1,mar:2,apr:3,mag:4,giu:5,lug:6,ago:7,set:8,ott:9,nov:10,dic:11};
function parseShiftRequest(x){const m=String(x.message||'');if(!/RICHIESTA (CAMBIO TURNO|FERIE|PERMESSO|RIPOSO)/.test(m))return null;const g=m.match(/Giorno:\s*([^\n]+).*?Turno attuale:\s*([^\n]+).*?Turno richiesto:\s*([^\n]+)/s);if(!g)return null;return{dateLabel:g[1].trim(),current:g[2].trim(),wanted:g[3].trim(),type:m.includes('RICHIESTA FERIE')?'Ferie':m.includes('RICHIESTA PERMESSO')?'Permesso':m.includes('RICHIESTA RIPOSO')?'Riposo':'Cambio turno'}}
function requestDate(x){const q=parseShiftRequest(x);if(!q?.dateLabel)return null;const m=String(q.dateLabel).toLowerCase().match(/(?:lun|mar|mer|gio|ven|sab|dom)?\s*(\d{1,2})\s+([a-zà]+)/i);if(!m)return null;const mon=MONTHS[m[2].slice(0,3)];if(mon===undefined)return null;const base=x.created_at?new Date(x.created_at):new Date(),years=[base.getFullYear()-1,base.getFullYear(),base.getFullYear()+1];let best=null,diff=Infinity;for(const y of years){const d=new Date(y,mon,+m[1],12);const n=Math.abs(d-base);if(n<diff){best=d;diff=n}}return best}
function weekInfo(d){const dow=d.getDay()||7,start=new Date(d);start.setDate(d.getDate()-dow+1);start.setHours(12,0,0,0);const end=new Date(start);end.setDate(start.getDate()+6);const z=n=>String(n).padStart(2,'0'),key=`${start.getFullYear()}-${z(start.getMonth()+1)}-${z(start.getDate())}`,month=x=>new Intl.DateTimeFormat('it-IT',{month:'long'}).format(x),label=start.getMonth()===end.getMonth()?`${start.getDate()}–${end.getDate()} ${month(start)}`:`${start.getDate()} ${month(start)} – ${end.getDate()} ${month(end)}`;return{key,label,start,end}}
function quickActions(x){return `<div class="request-quick-actions"><a class="primary center" href="review-change.html?id=${x.id}">Visualizza Turnazione</a><div><button class="primary smallbtn" data-normal="${x.id}" data-status="ACCETTATA">Accetta</button><button class="secondary smallbtn" data-normal="${x.id}" data-status="RIFIUTATA">Rifiuta</button></div></div>`}
function card(x){const quick=parseShiftRequest(x);if(mode==='open'&&quick)return `<article class="request-item open-request"><div class="request-meta">${esc(formatDateTime(x.created_at))} · <b>${esc(x.employee)}</b></div><div class="request-top">${statusBadge(x.status)} <span class="closed-badge" style="background:#ffd500;color:#003d7c">${esc(quick.type)}</span></div><div class="replybox"><small>${esc(quick.dateLabel)}</small><p><b>Attuale:</b> ${esc(quick.current)}<br><b>Richiesto:</b> ${esc(quick.wanted)}</p></div>${quickActions(x)}</article>`;return mode==='open'?`<article class="request-item open-request"><div class="request-meta">${esc(formatDateTime(x.created_at))} · <b>${esc(x.employee)}</b></div><div class="request-top">${statusBadge(x.status)}</div><p>${esc(x.message)}</p><div class="request-admin" style="display:grid;grid-template-columns:1fr 1fr;gap:8px"><button class="primary smallbtn" data-normal="${x.id}" data-status="ACCETTATA">Accetta</button><button class="secondary smallbtn" data-normal="${x.id}" data-status="RIFIUTATA">Rifiuta</button></div></article>`:`<article class="request-item closed-request"><div class="request-meta">${esc(formatDateTime(x.created_at))} · <b>${esc(x.employee)}</b></div><div class="request-top">${statusBadge(x.status)} <span class="closed-badge">Chiusa</span></div><p>${esc(x.message)}</p>${x.reply?`<div class="replybox"><b>Risposta Eurospin</b><p>${esc(x.reply)}</p></div>`:''}</article>`}
if(u){app.innerHTML=`<div class="app-shell"><div class="brandbar"></div><header class="topbar"><div class="toprow"><a class="back" href="index.html">‹ Turni</a><div class="brand-mini"><div class="mini-mark">E</div><div><strong>Richieste ricevute</strong><span>Solo account Eurospin</span></div></div><button class="logout" onclick="logout()">Esci</button></div></header><main class="main"><section class="hero inboxhero"><div class="eyebrow">Messaggi dipendenti</div><h2>Richieste</h2></section><div class="request-tabs"><button id="openTab" class="active">Aperte</button><button id="closedTab">Chiuse</button></div><div id="inbox" class="request-list"><div class="empty">Caricamento…</div></div></main></div>`;
const style=document.createElement('style');style.textContent=`.request-week{background:#fff;border:1px solid #d9e4eb;border-radius:18px;overflow:hidden;margin-bottom:12px}.request-week>summary{list-style:none;cursor:pointer;padding:13px 14px;background:#eef5fa;color:#003d7c;display:flex;align-items:center;justify-content:space-between}.request-week>summary::-webkit-details-marker{display:none}.request-week>summary b,.request-week>summary small{display:block}.request-week>summary b{font-size:16px}.request-week>summary small{font-size:10px;color:#667c89;margin-top:2px}.request-week[open]>summary{border-bottom:1px solid #d9e4eb}.request-week-body{padding:9px}.request-week-body .request-item{margin-bottom:8px}.request-week-body .request-item:last-child{margin-bottom:0}.request-quick-actions{display:grid;gap:8px}.request-quick-actions>a{display:block;text-align:center;text-decoration:none}.request-quick-actions>div{display:grid;grid-template-columns:1fr 1fr;gap:8px}`;document.head.appendChild(style);
function draw(){const list=all.filter(x=>mode==='open'?!x.replied_at:!!x.replied_at);document.getElementById('openTab').classList.toggle('active',mode==='open');document.getElementById('closedTab').classList.toggle('active',mode==='closed');const box=document.getElementById('inbox');if(!list.length){box.innerHTML='<div class="empty">Nessuna richiesta.</div>';return}const groups=new Map(),other=[];for(const x of list){const d=requestDate(x);if(!d){other.push(x);continue}const w=weekInfo(d);if(!groups.has(w.key))groups.set(w.key,{...w,items:[]});groups.get(w.key).items.push({x,d})}const ordered=[...groups.values()].sort((a,b)=>a.start-b.start),now=new Date();box.innerHTML=ordered.map(g=>{g.items.sort((a,b)=>a.d-b.d);const current=now>=g.start&&now<=g.end;return `<details class="request-week" ${mode==='open'||current?'open':''}><summary><div><b>${esc(g.label)}</b><small>${g.items.length} ${g.items.length===1?'richiesta':'richieste'}</small></div><span>⌄</span></summary><div class="request-week-body">${g.items.map(o=>card(o.x)).join('')}</div></details>`}).join('')+(other.length?`<details class="request-week" open><summary><div><b>Altre richieste</b><small>${other.length} ${other.length===1?'richiesta':'richieste'}</small></div><span>⌄</span></summary><div class="request-week-body">${other.map(card).join('')}</div></details>`:'');document.querySelectorAll('[data-normal]').forEach(b=>b.onclick=async()=>{const id=+b.dataset.normal,status=b.dataset.status,reply=status==='ACCETTATA'?'Richiesta accettata da Eurospin. Verrà applicata alla turnazione della settimana interessata.':'Richiesta rifiutata da Eurospin.';if(!confirm(status==='ACCETTATA'?'Accettare questa richiesta?':'Rifiutare questa richiesta?'))return;b.disabled=true;try{await api('close_request',{method:'POST',body:{id,status,reply}});await load()}catch(e){alert(e.message);b.disabled=false}})}
async function load(){try{const r=await api('list_requests');all=r.requests||[];draw()}catch(e){document.getElementById('inbox').innerHTML=`<div class="error">${esc(e.message)}</div>`}}document.getElementById('openTab').onclick=()=>{mode='open';draw()};document.getElementById('closedTab').onclick=()=>{mode='closed';draw()};load();}