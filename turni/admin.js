const u=requireUser(true),app=document.getElementById('app');let data,week=0;const fixMode=new URLSearchParams(location.search).get('fix')==='1';
async function boot(){if(!u)return;data=await loadSchedule();week=Math.max(0,data.weeks.length-1);if(fixMode)return renderFix();render()}
function hourRows(w){return Object.keys(w.schedule).map(n=>{const h=weekHours(w,n),e=Math.max(0,h-target(n));return `<div class="hours-row"><div><b>${esc(n)}</b><small>${esc(ROLES[n]||'')}</small></div><strong>${h}h</strong><span class="${e?'extra':''}">${FORF.has(n)?'forf.':e?`+${e}h`:'0'}</span></div>`}).join('')}
function validation(w){const x=validateWeek(w);return `<section class="validation ${x.length?'bad':'good'}"><div><strong>${x.length?`⚠️ ${x.length} problemi da risolvere`:'✅ Tutte le regole rispettate'}</strong></div>${x.length?`<ul>${x.map(v=>`<li>${esc(v)}</li>`).join('')}</ul>`:''}</section>`}
function renderFix(){let req;try{req=JSON.parse(localStorage.getItem('tm_fix_request')||'null')}catch{}if(!req){location.href='admin.html';return}const wi=data.weeks.findIndex(w=>w.dates.includes(req.date));if(wi<0){alert('Settimana non trovata');location.href='inbox.html';return}week=wi;const w=data.weeks[wi],di=w.dates.indexOf(req.date),original=w.schedule[req.employee][di];w.schedule[req.employee][di]=req.wanted;const problems=validateWeek(w),key=`${req.employee}|${req.date}`;app.innerHTML=`<style>.fix-shell{min-height:100vh;background:#f4f7fa;padding:10px}.fix-head{background:#0067b1;color:white;border-radius:16px;padding:14px 16px;margin-bottom:10px}.fix-head b{display:block;font-size:20px}.fix-grid{overflow:auto;background:#fff;border-radius:14px;border:1px solid #dbe5ed}.fix-table{border-collapse:collapse;min-width:900px;width:100%}.fix-table th,.fix-table td{border:1px solid #dbe5ed;padding:8px;font-size:11px;text-align:center}.fix-table th{background:#003d7c;color:#fff;position:sticky;top:0}.fix-table td:first-child{font-weight:900;text-align:left;position:sticky;left:0;background:#fff}.fix-table .target{background:#ffd500!important;color:#003d7c;font-weight:950;box-shadow:inset 0 0 0 3px #c59f00}.fix-editor{background:#fff;margin-top:10px;border-radius:14px;padding:12px}.fix-editor select,.fix-editor input{width:100%;padding:10px;border:1px solid #ccd9e3;border-radius:10px;margin:4px 0 8px}.fix-times{display:grid;grid-template-columns:1fr 1fr;gap:8px}.fix-actions{display:grid;grid-template-columns:1fr 1fr;gap:8px}.fix-actions button{padding:13px;border-radius:12px;font-weight:950}.fix-save{border:0;background:#0067b1;color:#fff}.fix-cancel{background:#fff;border:1px solid #ccd9e3;color:#003d7c}.fix-problems{background:#fff3b0;color:#4f4300;border-radius:12px;padding:10px;margin:10px 0;font-size:12px}</style><div class="fix-shell"><div class="fix-head"><small>Correzione cambio turno</small><b>${esc(req.employee)} · ${esc(fmtDate(req.date))}</b><span>${esc(original)} → ${esc(w.schedule[req.employee][di])}</span></div>${problems.length?`<div class="fix-problems"><b>Da sistemare:</b><br>${problems.map(esc).join('<br>')}</div>`:''}<div class="fix-grid"><table class="fix-table"><thead><tr><th>Dipendente</th>${w.dates.map(d=>`<th>${esc(fmtDate(d))}</th>`).join('')}</tr></thead><tbody>${Object.keys(w.schedule).map(n=>`<tr><td>${esc(n)}</td>${w.dates.map((d,i)=>`<td class="${n===req.employee&&i===di?'target':''}">${esc(w.schedule[n][i]||'—')}</td>`).join('')}</tr>`).join('')}</tbody></table></div><div class="fix-editor"><b>Modifica solo questo turno</b><select id="fixQuick">${SHIFT_OPTIONS.map(s=>`<option ${s===w.schedule[req.employee][di]?'selected':''}>${esc(s)}</option>`).join('')}</select><button class="secondary full" id="fixApplyQuick">Usa turno rapido</button><div class="fix-times"><div><label>Inizio 1</label><input id="f1s" type="time" step="900"></div><div><label>Fine 1</label><input id="f1e" type="time" step="900"></div><div><label>Inizio 2</label><input id="f2s" type="time" step="900"></div><div><label>Fine 2</label><input id="f2e" type="time" step="900"></div></div><button class="secondary full" id="fixApplyCustom">Usa orario personalizzato/spezzato</button><div class="fix-actions"><button class="fix-cancel" id="fixBack">Annulla</button><button class="fix-save" id="fixConfirm">${problems.length?'Ricontrolla':'Conferma e applica'}</button></div></div></div>`;
const redraw=()=>renderFix();document.getElementById('fixApplyQuick').onclick=()=>{w.schedule[req.employee][di]=document.getElementById('fixQuick').value;data.weeks[wi]=w;redraw()};document.getElementById('fixApplyCustom').onclick=()=>{const a=document.getElementById('f1s').value,b=document.getElementById('f1e').value,c=document.getElementById('f2s').value,z=document.getElementById('f2e').value;if(!a||!b||hm(b)<=hm(a))return alert('Controlla la prima fascia');let s=`${a}-${b}`;if(c||z){if(!c||!z||hm(z)<=hm(c)||hm(c)<hm(b))return alert('Controlla la seconda fascia');s+=` / ${c}-${z}`}w.schedule[req.employee][di]=s;data.weeks[wi]=w;redraw()};document.getElementById('fixBack').onclick=()=>{w.schedule[req.employee][di]=original;localStorage.removeItem('tm_fix_request');location.href='inbox.html'};document.getElementById('fixConfirm').onclick=async()=>{const p=validateWeek(w);if(p.length){renderFix();return}data.overrides=data.overrides||{};delete data.overrides[key];await api('save_schedule',{method:'POST',body:{data}});await api('close_request',{method:'POST',body:{id:req.requestId,status:'ACCETTATA',reply:`Cambio turno accettato e corretto: ${w.schedule[req.employee][di]}`}});localStorage.removeItem('tm_fix_request');location.href='inbox.html'} }
function render(){const w=data.weeks[week],names=Object.keys(w.schedule);app.innerHTML=`<div class="app-shell"><div class="brandbar"></div><header class="topbar"><div class="toprow"><a class="back" href="index.html">‹ Turni</a><div class="brand-mini"><div class="mini-mark">E</div><div><strong>Amministratore</strong><span>Solo account Eurospin</span></div></div><button class="logout" onclick="logout()">Esci</button></div></header><main class="main"><section class="hero adminhero"><div class="eyebrow">Gestione turni</div><h2>Modifica turni</h2></section><div class="weeknav"><button id="prev">‹</button><div class="chips">${data.weeks.map((x,i)=>`<button class="chip ${i===week?'active':''}" data-w="${i}">${x.label}</button>`).join('')}</div><button id="next">›</button></div>${validation(w)}<div class="section-title"><h3>Ore settimana</h3></div><div class="hours-table">${hourRows(w)}</div><section class="admin-editor"><div class="field"><label>Dipendente</label><select id="person">${names.map(n=>`<option>${esc(n)}</option>`).join('')}</select></div><div class="field"><label>Giorno</label><select id="day">${w.dates.map((d,i)=>`<option value="${i}">${fmtDate(d)}</option>`).join('')}</select></div><div class="field"><label>Turno rapido</label><select id="shift">${SHIFT_OPTIONS.map(s=>`<option>${esc(s)}</option>`).join('')}</select></div><button class="primary" id="apply">Applica turno rapido</button><div class="custom-shift"><b>Turno personalizzato</b><div class="time-grid"><div class="field"><label>Inizio</label><input id="start1" type="time" step="900" value="07:00"></div><div class="field"><label>Fine</label><input id="end1" type="time" step="900" value="14:00"></div><div class="field"><label>Inizio 2</label><input id="start2" type="time" step="900"></div><div class="field"><label>Fine 2</label><input id="end2" type="time" step="900"></div></div><button class="secondary full" id="applyCustom">Applica personalizzato</button></div></section><div class="employee-grid">${names.map(n=>`<section class="employee-week"><div class="employee-head"><strong>${esc(n)}</strong></div><div class="mini-days">${w.dates.map((d,i)=>{const k=`${n}|${d}`,ov=data.overrides?.[k];return `<button class="editcell" data-p="${esc(n)}" data-d="${i}" style="${ov?'background:#ffd500;color:#003d7c':''}"><span>${fmtDate(d).slice(0,3)}</span><b>${esc(w.schedule[n][i]||'—')}</b>${ov?'<small>Accettato da amministratore</small>':''}</button>`}).join('')}</div></section>`).join('')}</div><div class="sticky-actions"><button class="primary" id="publish">Pubblica turni</button></div></main></div>`;document.querySelectorAll('[data-w]').forEach(b=>b.onclick=()=>{week=+b.dataset.w;render()});document.getElementById('prev').onclick=()=>{week=Math.max(0,week-1);render()};document.getElementById('next').onclick=()=>{week=Math.min(data.weeks.length-1,week+1);render()};document.getElementById('apply').onclick=()=>{const p=document.getElementById('person').value,d=+document.getElementById('day').value;w.schedule[p][d]=document.getElementById('shift').value;render()};document.getElementById('applyCustom').onclick=()=>{const p=document.getElementById('person').value,d=+document.getElementById('day').value,a=document.getElementById('start1').value,b=document.getElementById('end1').value,c=document.getElementById('start2').value,z=document.getElementById('end2').value;if(!a||!b||hm(b)<=hm(a))return alert('Controlla la prima fascia');let s=`${a}-${b}`;if(c||z){if(!c||!z||hm(z)<=hm(c)||hm(c)<hm(b))return alert('Controlla la seconda fascia');s+=` / ${c}-${z}`}w.schedule[p][d]=s;render()};document.getElementById('publish').onclick=async()=>{const problems=data.weeks.flatMap(x=>validateWeek(x));const overridden=Object.keys(data.overrides||{}).length;if(problems.length&&!overridden)return alert(`Non posso pubblicare: ${problems.length} problemi da risolvere.`);await api('save_schedule',{method:'POST',body:{data}});alert('Turni pubblicati.')}}
boot();