const u=requireUser(true),app=document.getElementById('app');let data,week=0;async function boot(){if(!u)return;data=await loadSchedule();render()}function render(){const w=data.weeks[week],names=Object.keys(w.schedule);app.innerHTML=`<div class="app-shell"><div class="brandbar"></div><header class="topbar"><div class="toprow"><a class="back" href="index.html">‹ Turni</a><div class="brand-mini"><div class="mini-mark">E</div><div><strong>Amministratore</strong><span>Solo account Eurospin</span></div></div><button class="logout" onclick="logout()">Esci</button></div></header><main class="main"><section class="hero adminhero"><div class="eyebrow">Gestione turni</div><h2>Modifica semplice</h2><p>Scegli settimana, persona, giorno e turno. Nessun codice.</p></section><div class="chips">${data.weeks.map((x,i)=>`<button class="chip ${i===week?'active':''}" data-w="${i}">${x.label}</button>`).join('')}</div><section class="admin-editor"><div class="field"><label>Dipendente</label><select id="person">${names.map(n=>`<option>${esc(n)}</option>`).join('')}</select></div><div class="field"><label>Giorno</label><select id="day">${w.dates.map((d,i)=>`<option value="${i}">${fmtDate(d)}</option>`).join('')}</select></div><div class="field"><label>Turno</label><select id="shift">${SHIFT_OPTIONS.map(s=>`<option>${esc(s)}</option>`).join('')}</select></div><button class="primary" id="apply">Applica modifica</button><div class="notice">Queste modifiche restano come bozza su questo iPhone. Per renderle ufficiali per tutti, dopo il controllo puoi farmi pubblicare la turnazione sull’app.</div></section><div class="section-title"><h3>Anteprima settimana</h3><span>${w.label}</span></div><div class="employee-grid">${names.map(n=>`<section class="employee-week"><div class="employee-head"><div><strong>${esc(n)}</strong><small>${esc(ROLES[n]||'')}</small></div></div><div class="mini-days">${w.dates.map((d,i)=>`<button class="editcell" data-p="${esc(n)}" data-d="${i}"><span>${fmtDate(d).slice(0,3)}</span><b>${esc(w.schedule[n][i]||'—')}</b></button>`).join('')}</div></section>`).join('')}</div><div class="sticky-actions"><button class="secondary" id="reset">Ripristina ufficiale</button><button class="primary" id="save">Salva bozza</button></div></main></div>`;document.querySelectorAll('[data-w]').forEach(b=>b.onclick=()=>{week=+b.dataset.w;render()});document.querySelectorAll('.editcell').forEach(b=>b.onclick=()=>{document.getElementById('person').value=b.dataset.p;document.getElementById('day').value=b.dataset.d;document.getElementById('shift').value=w.schedule[b.dataset.p][+b.dataset.d]||'—';scrollTo({top:150,behavior:'smooth'})});document.getElementById('apply').onclick=()=>{const p=document.getElementById('person').value,d=+document.getElementById('day').value,s=document.getElementById('shift').value;w.schedule[p][d]=s;render()};document.getElementById('save').onclick=()=>{localStorage.setItem('tm_admin_draft',JSON.stringify(data));alert('Bozza salvata su questo iPhone.')};document.getElementById('reset').onclick=()=>{localStorage.removeItem('tm_admin_draft');location.reload()}}boot();