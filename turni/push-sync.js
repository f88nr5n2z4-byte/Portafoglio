(async()=>{try{const u=getSession();if(!u||!('serviceWorker'in navigator)||!('PushManager'in window)||!('Notification'in window)||Notification.permission!=='granted')return;const reg=await navigator.serviceWorker.ready;const sub=await reg.pushManager.getSubscription();if(!sub)return;await api('save_push_subscription',{method:'POST',body:{subscription:sub.toJSON()}})}catch(e){console.warn('push account sync',e)}})();