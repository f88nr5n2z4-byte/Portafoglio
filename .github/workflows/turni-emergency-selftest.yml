name: Turni emergency selftest
on:
  push:
    paths:
      - 'turni-v2/EMERGENCY-SELFTEST.txt'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Invoke emergency selftest
        run: |
          set -euo pipefail
          curl --fail-with-body --silent --show-error 'https://dlqrhteqodkdkvmrwktu.supabase.co/functions/v1/turni-mixed-absence-selftest-20260914'
